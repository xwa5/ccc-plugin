# Baidu CCC Plugin

百度 Coding Config 管理工具的 Claude Plugin。

## 📦 插件简介

这是 `baidu-ccc-dev` CLI 工具的 Claude Code Plugin 包装。它为 Claude 提供了智能的配置同步能力，让团队成员可以轻松地安装和管理团队统一的 Claude 配置。

**核心功能**：
- 🚀 通过斜杠命令快速安装团队配置
- 🤖 Claude 智能识别配置同步需求
- 📋 查看当前配置状态
- 🔄 自动检测并解决配置冲突

## 🛠️ 前置要求

### 1. 安装 baidu-ccc-dev CLI 工具

```bash
npm install -g baidu-ccc-dev
```

### 2. 验证安装

```bash
baidu-ccc-dev --version
```

## 📥 安装插件

### 方式 1: 从 GitHub Marketplace 安装（推荐）

```bash
# 步骤 1：添加 Marketplace
/plugin marketplace add xwa5/ccc-plugin

# 步骤 2：安装插件
/plugin install baidu-ccc@xwa5-ccc-plugin
```

### 方式 2: 本地安装（开发测试）

```bash
# 步骤 1：克隆仓库到本地
git clone https://github.com/xwa5/ccc-plugin.git
cd ccc-plugin

# 步骤 2：添加本地 Marketplace
/plugin marketplace add ./path/to/ccc-plugin

# 步骤 3：安装插件
/plugin install baidu-ccc@ccc-plugin

# 或者直接复制到 Claude plugins 目录
cp -r ccc-plugin ~/.claude/plugins/baidu-ccc
```

### 方式 3: 从 Git URL 安装

```bash
# 步骤 1：直接添加 Git 仓库作为 Marketplace
/plugin marketplace add https://github.com/xwa5/ccc-plugin.git

# 步骤 2：安装插件
/plugin install baidu-ccc@xwa5-ccc-plugin
```

## ✅ 验证安装

### 1. 检查已安装的插件

```bash
# 打开插件管理器
/plugin

# 导航到 "Installed" 标签页
# 应该看到：baidu-ccc@... (Enabled)
```

### 2. 测试斜杠命令

```bash
# 运行帮助命令查看可用命令
/help

# 应该看到：
# /ccc-install - 安装百度团队的 Coding Config 配置
# /ccc-status  - 查看当前 Coding Config 配置状态

# 测试命令
/ccc-status
```

### 3. 测试智能识别

在 Claude 对话中输入：
```
"帮我安装团队的 Claude 配置"
```

Claude 应该自动识别并运行 `baidu-ccc-dev install`。

## 🎯 使用方式

### 方式 1: 斜杠命令（显式调用）

在 Claude Code 对话中输入：

```
/ccc-install   # 安装团队配置
/ccc-status    # 查看配置状态
```

### 方式 2: 自然语言（智能识别）

直接告诉 Claude 你的需求，它会自动识别并使用此工具：

```
"帮我安装团队的 Claude 配置"
"我的配置是最新的吗？"
"同步一下团队的 coding config"
```

Claude 会自动：
1. 检查 `baidu-ccc-dev` 是否已安装
2. 运行相应的 CLI 命令
3. 帮助你处理冲突
4. 总结变更内容

## 📖 工作流程示例

### 场景 1: 首次安装配置

```
👤 你: "帮我安装团队的 Claude 配置"

🤖 Claude:
1. 检查工具安装状态...
2. 运行 `baidu-ccc-dev install`
3. 请选择配置级别：
   - 个人级 (~/.claude/)
   - 项目级 (./.claude/)
4. 下载并安装配置...
5. ✅ 成功！新增了 5 个 commands，3 个 skills
```

### 场景 2: 检查更新

```
👤 你: "我的配置是最新的吗？"

🤖 Claude:
1. 运行 `baidu-ccc-dev status`
2. 当前配置来源：https://...
3. 建议运行更新检查...
4. 发现 2 个文件有更新，是否安装？
```

### 场景 3: 处理冲突

```
👤 你: "/ccc-install"

🤖 Claude:
1. 检测到 3 个冲突文件
2. commands/deploy.md (本地和远程都有修改)
   - 查看差异
   - 保留本地版本
   - 使用远程版本
3. 你选择: 查看差异
4. [显示差异]
5. 你选择: 使用远程版本
```

## 📁 插件结构

```
ccc-plugin/
├── .claude-plugin/
│   ├── plugin.json          # 插件元数据
│   └── marketplace.json     # Marketplace 配置（用于发布）
├── commands/
│   ├── install.md           # /ccc-install 命令
│   └── status.md            # /ccc-status 命令
├── skills/
│   └── baidu-ccc-helper/
│       └── SKILL.md         # 智能识别 Skill
└── README.md
```

## 🔧 配置说明

### Commands（斜杠命令）

- `/ccc-install` - 安装/更新团队配置
- `/ccc-status` - 查看当前配置状态

### Skills（智能助手）

- `baidu-ccc-helper` - 自动识别配置同步需求，提供智能引导

### 配置作用域

安装插件时可以选择不同的作用域：

| 作用域 | 位置 | 适用范围 | 推荐场景 |
|-------|------|---------|---------|
| **user** | `~/.claude.json` | 你的所有项目 | 个人使用 |
| **project** | `.claude/settings.json` | 当前项目（可提交到 Git） | 团队共享 |
| **local** | 本地配置，不共享 | 仅当前项目 | 测试使用 |

**推荐做法**：
- 个人使用：安装到 **user** 作用域
- 团队协作：安装到 **project** 作用域并提交配置文件

## 🤝 与 baidu-ccc-dev 的关系

```
┌─────────────────────────────────────┐
│  ccc-plugin (Claude Plugin)         │
│  ├── Commands (Markdown)            │
│  ├── Skills (Markdown)              │
│  └── 指导 Claude 如何调用 CLI       │
└──────────────┬──────────────────────┘
               │ 通过 Bash 工具调用
               ↓
┌─────────────────────────────────────┐
│  baidu-ccc-dev (npm CLI)            │
│  ├── 下载配置                        │
│  ├── 检测冲突                        │
│  ├── 合并配置                        │
│  └── 更新记录                        │
└─────────────────────────────────────┘
```

**关键点**：
- Plugin 不包含任何逻辑代码，只有 Markdown 配置
- 实际工作由 `baidu-ccc-dev` CLI 完成
- Claude 通过 Bash 工具调用 CLI
- Plugin 只是告诉 Claude "何时"和"如何"使用 CLI
- **不使用 MCP**，纯 Commands + Skills 机制

## ❓ 常见问题

### Q: 为什么需要两个项目？

A:
- `baidu-ccc-dev` - 独立的 CLI 工具，可单独使用
- `ccc-plugin` - Claude 集成层，让 Claude 知道如何使用 CLI

### Q: 我必须使用 Plugin 吗？

A: 不是。你可以直接使用 `baidu-ccc-dev` CLI。Plugin 只是让 Claude 更智能地帮你使用它。

### Q: Plugin 会自动安装 baidu-ccc-dev 吗？

A: 不会。你需要手动安装 `npm install -g baidu-ccc-dev`。

### Q: 这个 Plugin 不使用 MCP 吗？

A: 对，完全不使用 MCP。它只使用 Commands 和 Skills 机制，纯 Markdown 配置。

### Q: 如何卸载插件？

A:
```bash
# 卸载插件
/plugin uninstall baidu-ccc@<marketplace-name>

# 移除 Marketplace
/plugin marketplace remove <marketplace-name>
```

### Q: 插件安装后命令不显示？

A:
```bash
# 清除缓存
rm -rf ~/.claude/plugins/cache

# 重启 Claude Code
exit
claude

# 重新安装插件
/plugin install baidu-ccc@<marketplace-name>
```

## 📝 开发和贡献

### 修改 Commands

编辑 `commands/*.md` 文件，修改斜杠命令的提示词。

### 修改 Skills

编辑 `skills/baidu-ccc-helper/SKILL.md`，调整 Claude 的识别和引导逻辑。

### 本地测试

1. 修改插件文件后，添加为本地 Marketplace：
   ```bash
   /plugin marketplace add ./path/to/ccc-plugin
   ```

2. 安装插件：
   ```bash
   /plugin install baidu-ccc@ccc-plugin
   ```

3. 测试功能后，如需更新：
   ```bash
   /plugin marketplace update ccc-plugin
   ```

4. 清除缓存重新测试：
   ```bash
   rm -rf ~/.claude/plugins/cache
   ```

### 发布流程

1. 将插件推送到 GitHub 仓库
2. 创建 `marketplace.json`（见下节）
3. 用户通过 Git URL 添加 Marketplace
4. 用户安装插件

## 📄 许可证

MIT

## 🔗 相关链接

- [baidu-ccc-dev CLI 工具](../ccc)
- [Claude Code Plugins 文档](https://code.claude.com/docs/en/plugins.md)
- [Plugin Marketplaces 文档](https://code.claude.com/docs/en/plugin-marketplaces.md)
